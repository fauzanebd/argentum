{
  "name": "@argentum/design-tokens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Canonical design tokens. Generates the dashboard's CSS variables and the backend's Go report theme.",
  "scripts": {
    "build": "node scripts/generate.mjs",
    "check": "node scripts/generate.mjs --check",
    "lint": "node scripts/generate.mjs --check",
    "palette": "node scripts/palette.mjs"
  },
  "files": [
    "tokens.json",
    "lib",
    "scripts"
  ]
}
