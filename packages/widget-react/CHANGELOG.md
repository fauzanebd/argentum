# Changelog

Written by [changesets](../../.changeset/README.md); do not edit by hand. Each
entry is what changed for somebody integrating this package, which is why the
lines describe the API rather than the commits.

Versions follow SemVer against the `/api/embed` contract: a major is a change to
the `init()` options or to the event names.
